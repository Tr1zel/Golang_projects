#### **Цель:**

Создать **веб-приложение**, которое позволяет **создавать, хранить, редактировать и искать заметки**. Заметки можно **категоризировать**, **тегировать**, **делать приватными/публичными**, **искать по содержимому**. Всё это **с веб-интерфейсом** и **хранением данных в PostgreSQL**.

---

### 🔍 Подробное ТЗ:

#### **Функциональные требования:**
**Веб-интерфейс:**
    
    - Главная страница: список публичных заметок.
    - Страница добавления заметки.
    - Страница просмотра заметки.

---

### 🧰 Технологии:

- `net/http` — для веб-сервера.
- `html/template` — для рендеринга HTML.
- `database/sql` или `github.com/jmoiron/sqlx` — для работы с PostgreSQL.
- `github.com/jackc/pgx/v5` — драйвер PostgreSQL.
- `time` — для работы с датами.
- `context` — для таймаутов и управления запросами.

---

### ✅ Польза:

- **Работа с базой данных** — PostgreSQL.
- **HTTP-сервер и маршрутизация**.
- **Веб-интерфейс** и **шаблоны**.
- **Полезный инструмент**, который можно использовать самому (как личный блокнот).
- **Покажет**, что ты умеешь:
    - Писать **бэкенд**.
    - Работать с **базой данных**.
    - **Проектировать API**.
    - **Отображать данные** в вебе.

