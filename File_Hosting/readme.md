#### **Цель:**

Создать **HTTP-сервер**, который позволяет **загружать файлы**, **получать к ним доступ по короткому уникальному URL**, и **хранить файлы** (в памяти или на диске).


---
#### **Функциональные требования:**

- `POST /upload` — добавить файл.
Возвращает короткий Id и URL для доступа к файлу
Пример:
```
{
  "id": "abc123",
  "url": "http://localhost:8080/abc123"
}
```
- `/get/{Id}` — скачать файл.
Если файл не найден возвращает `404 Not Found`


### 🧰 Технологии:

- `net/http` — для HTTP-сервера.
- `os`, `path/filepath` — для сохранения файлов на диск.
- `crypto/rand` или `math/rand` — для генерации уникального ID.
- `time`, `context` — для TTL и таймаутов.
- `io` — для чтения/записи файлов.